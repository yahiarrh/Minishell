#include "../minishell.h"

static int	syntx(t_token *list)
{
	if ()
}

void	parcer(t_token **list, t_gt **head)
{
	t_general	*general;
	t_token		**tmp;

	tmp = list;
	while ((*list))
	{
		if(!syntx(*list))
		{
			
			return ;
		}
	}
	get_ptr(list, 0, 0);
}
